'use client'

/**
 * Profile completion is optional and must never block reporting.
 * The modal remains available for explicit use; auto-prompts were removed.
 */
export function CompleteProfileModalWrapper() {
  return null
}
