# Supervisor Operations Guide

1. `/ops/supervisor` — actionable counts only.
2. Click a metric → filtered Smart Work Queue.
3. Duplicate reviews pending → review side-by-side on case detail.
4. Workload list shows open cases by assignee UID (no public rankings).
5. Use `/ops/map` for spatial triage.
